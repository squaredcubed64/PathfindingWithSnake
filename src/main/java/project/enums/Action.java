package project.enums;

public enum Action {
	// Turn left and move in that direction
	LEFT,
	// Turn right and move in that direction
	RIGHT,
	// Simply move forward
	FORWARD,
	// After reaching FOOD
	WAIT
}
